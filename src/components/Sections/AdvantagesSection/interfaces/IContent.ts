export interface IContent {
    other: {
        title: string,
        descriptions: string[],
        block: string
    }
    we: {
        title: string,
        descriptions: string[],
        block: string
    }
}